const arreglo = ['Carlos'];

const getNames = (arr) => {
  arr.forEach((element) => {
    console.log(element);
  });
};

const sayHello = () => {
  arr.forEach((element) => {
    console.log(`Hola,${element}`);
  });
};

getNames();
